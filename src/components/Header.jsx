import React from 'react'
import { Typography } from '@material-ui/core'

class Header extends React.Component {
  render() {
    return (
      <Typography variant="body2" id="header-title">
        TODO list
      </Typography>
    )
  }
}

export default Header
